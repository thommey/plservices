#ifndef TREE_H_
#define TREE_H_

struct treenode {
	void *data;
	struct treenode *next[27];
};

typedef struct treenode tree_t;

void memreport_tree(void);
void *tree_create(void);
void tree_insert(tree_t *tree, const char *str, void *data);
void *tree_get(tree_t *tree, const char *str);

#endif // TREE_H_
